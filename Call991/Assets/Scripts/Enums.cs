public enum GameScenes
{
    Menu = 0,
    Level = 1,
    OpenScene = 2,
}

public enum DialogAppearType
{
    Immediate,
    Letters,
    Word,
    Fade,
}

public enum AudioSourceType
{
    Master,
    Voice,
    Music,
    Sfx,
}